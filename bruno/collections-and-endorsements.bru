meta {
  name: Collections and endorsements
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/xrpc/org.hypercerts.feed.getFeed
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "feedId": "{{feedId}}",
    "params": {
      "$type": "{{feedParamsType}}",
      "viewerDid": "{{viewerDid}}",
      "kinds": ["collection.create", "endorsement.award"]
    },
    "limit": {{limit}},
    "cursor": "{{cursor}}"
  }
}
